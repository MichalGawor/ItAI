b'' b'' b'[summary elided]' b"the system as described here can't possibly work. what happens when" b'someone plugs the above ciphertext into a receiving chip? to get m' b'the receiving chip needs k_p; to get k_p the receiving chip needs u_c.' b'the only information it can work with is c. if u_c can be computed' b'from c then the system is cryptographically useless and the ``key' b"escrow'' is bullshit. otherwise how is a message decrypted?" b'' b"the description of the chip's operation evidently leaves out some of the" b'key management aspects. either the k_p is the secret key corresponding' b'to a public key which is broadcast at message initiation, or it is the' b'result of a diffie-hellman key exchange or something similar. either' b"way there must be some protocols beyond those described here. it isn't" b'clear whether they are implemented in the clipper wiretap chip or must' b'be provided by other system components.' b'' b'hal finney' 