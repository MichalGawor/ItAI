b'' b'hello,' b"i've recently had povray draw about sample files. the problem is that" b'i accidently erased the command in my povray.def that made the image a targas' b'file. so now the files are the dump format. how do i fix these files with out' b'having to re-trace them? by fix i mean, turn them into targas. thanks in' b'advance.' b'-jerry' b'--' b'#################################################################' b'jerry m. lineberry' b'#################################################################' 