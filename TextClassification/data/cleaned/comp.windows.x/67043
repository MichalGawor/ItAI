b'' b'#' b"#i'm trying to write some code that lets me draw lines and do rubber-band" b"#boxes in motif/x. i'm running on an 8-bit display for which i've created a" b'#colormap and am using almost all of the colors. i want to draw the lines' b"#in a drawing area widget a widget in which i'm displaying a bitmap using" b"#xputimage(). if doesn't matter if the lines i draw interactively stay" b'#around when the window is refreshed.' b'#' b'#currently, to draw interactively, i begin with:' b'#' b'# drawindex is an colortable index i reserve for the foreground b'# my_default_bg_color is the color index for the background of my image b'# palette_colors[drawindex].red palette_colors[my_default_bg_color].red;' b'# palette_colors[drawindex].green palette_colors[my_default_bg_color].green;' b'# palette_colors[drawindex].blue palette_colors[my_default_bg_color].blue;' b'# xstorecolors( mydisplay, my_cmap, &palette_colors[drawindex], b'# xflush( mydisplay);' b'#' b'# xsetfunction( mydisplay, gc, gxxor);' b'# xsetforeground( mydisplay, gc, drawindex);' b'#' b'#then to draw i do:' b'#' b'# xdrawline( mydisplay, xtwindow( drawingarea1), gc, x1, y1, x2, y2);' b'# xflush( mydisplay);' b'#' b"#and when i'm all done, to return things to normal i do:" b'#' b'# xsetfunction( mydisplay, gc, gxcopy);' b'#' b'#' b"#what i'd like to happen is for the lines i draw to be the inverse of" b"#whatever i'm drawing over. instead what happens is i get white lines. if" b'#the lines are over a white background nothing shows up. if the lines are' b"#over a black area nothing shows up! it's very strange. but the gxxor" b'#function seems right since if i do a rubber-banding box, it erases and' b'#redraws itself correctly (ie. not disturbing the underlying image).' b'#' b"#any suggestions what i'm doing wrong?" b'#' b'' b'i believe for this to work on a color display you must set the foreground of' b'the gc to be (foreground^background).' b'' b'patrick l. mahan' b'' b'' b'waking a person unnecessarily should not be considered lazarus long' b'a capital crime. for a first offense, that is from the notebooks of' b'lazarus long' 