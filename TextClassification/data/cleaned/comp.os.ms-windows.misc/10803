b'' b'' b'' b'the microsoft windows device driver kit (ddk) documentation (blurb)' b'states that the user must separately acquire the microsoft windows' b'software development kit (sdk), which contains the windows tools' b'to build the drivers. if we are using borland c++ with application' b'frameworks, is this necessary? we also have a copy of the microsoft' b"press programmer's reference library (books) which normally comes" b'with the sdk, and microsoft macro assembler why is the sdk' b'neccessary for development of virtual device drivers?' b'' b'- steven murray' b'(posted on behalf of brian davies)' b'' 