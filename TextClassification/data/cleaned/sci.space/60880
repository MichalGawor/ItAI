b'' b'"clear caution warning memory. verify no unexpected' b'errors. i am wondering what an "expected error" might' b'be. sorry if this is a really dumb question, but' b'' b' parity errors in memory or previously known conditions that were waivered.' b' "yes that is an error, but we already knew about it"' b" i'd be curious as to what the real meaning of the quote is." b'' b' tom' b'' b'' b"my understanding is that the 'expected errors' are basically" b'known bugs in the warning system software things are checked' b"that don't have the right values in yet because they aren't" b'set till after launch, and suchlike. rather than fix the code' b'and possibly introduce new bugs, they just tell the crew' b"'ok, if you see a warning no. before liftoff, ignore it'." b'' b'- jonathan' b'' b'' 