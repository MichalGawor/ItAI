b'' b'' b'|' b'| hi,' b'|' b'|' b'|' b'| could someone explain to me what exactly the xlib functions' b'|' b'| xcreateregion, xpolygonregion, xpointinregion do, or to' b'|' b'| suggest a ftp site where i can find programs using these functions.' b'|' b'|' b'| ps: the man pages does not explain much b'|' b'|' b'| thank you,' b'| leo.' b'|' b'|' b'|' b'|' b'|' b'|' b'| b'| b'| \t\t\t\t\tdept. of computing' b'| b'i use those routines in a widget which only redraws exposed areas.' b"here's a small piece of code that uses those routines" b'' b'static void redisplay(pw, ev, region)' b'xtpmanagerwidget pw;' b'xexposeevent *ev;' b'region region;' b'' b'/* pp-exposure_region is an xregion b'xtpmanagerpart *pp &pw-plotter;' b'pp-exposure_region xcreateregion();' b'/* region is passed via an expose event b'if(region null)' b'{' b'if (ev-send_event) synthetic event sent by widget it wants a full redraw*/' b'{' b'exporect.x pw-core.x;' b'exporect.y pw-core.y;' b'exporect.width pw-core.width;' b'exporect.height pw-core.height;' b'}' b'else' b'{' b'exporect.x ev-x;' b'exporect.y ev-y;' b'exporect.width ev-width;' b'exporect.height ev-height;' b'}' b'' b'stuff deleted' b'' b'/* region now contains the exposure region b'xunionrectwithregion(&exporect,pp-exposure_region,pp-exposure_region);' b'}' b'else' b'xunionregion region, pp-exposure_region, pp-exposure_region);' b'stuff deleted' b'/* we need to clip on expose region for redrawing the grid b'xsetregion(xtdisplayofobject(pw), pp-plotarea_gc,pp-exposure_region);' b'stuff deleted' b'/* check to see if child is exposed b'if (xrectinregion (pw-plotter.exposure_region,' b'child-core.x b'child-core.y b'child-core.width b'child-core.height b'' b'so, i build up a region of exposures so that when i service my container' b'widget redraw of rectobj children, i only redraw what need to be redrawn.' b'' b'it works for me, hope this helps you.' b'brian' b'' b'--' b"brian dealy it not knowing where it's at" b"!uunet!dftsrv!kong!dealy where it's not at... b.dylan" 