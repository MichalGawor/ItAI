b'' b":it's ok as long as you trust the end-user to stay out of your application" b":program or as long as it runs in a system where the user can't get to it." b':otherwise, you can\'t stop him from finding the "load a module" code in your' b':program, and simply bypassing the check for a valid module. the devious user' b':can either modify the object code before running the program, or he can run' b':the program under a debugger and change the instructions in memory.' b':there is a way to foil debuggers, by clearing the single step' b':interrupt, on which a debugger depends, every few instructions in' b':the sensitive areas of the code.' b'' b'this assumes the person is using the hardware debug instruction of an x86' b"type processor. it can be negated by nop'ing the clear debug instruction," b'or by running the code on a machine simulator, like one i wrote as a senior' b'project in college. it can bypass and trace practically anything one could' b'write in software. kind of like being on a star trek holideck b'' b'--' b'kenneth ng' b'"all this might be an elaborate simulation running in a little device sitting' b'on someone\'s table" j.l. picard: st:tng' 