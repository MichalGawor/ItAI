b'' b'' b"we have a problem with 'makedepend': it doesn't know how to correctly process" b'#if clause in c preprocessor statements. it evaluates everything to' b'true no matter what the conditionals are.' b'' b'[...]' b'' b"has anybody come up with some solution/fix to 'makdepend' other than" b'writeyourownparser... and willing to share it with us?' b'' b"rewrite 'makedepend' to use 'cc -m' or 'gcc -m' or 'gcc -mm' (which is" b"the one i prefer, since system headers don't change that often) for" b"generating the actual dependencies. you'll still need to write the" b"parts that edit the actual makefile. i think there's a program in the" b'berkeley distributions called mkdep that will do essentially this.' b'--' b'=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=-=' b'vivek khera, gradual student/systems guy department of computer science' b'(mime mail accepted) durham, nc 