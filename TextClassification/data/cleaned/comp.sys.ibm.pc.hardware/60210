b'' b'date: apr b'everybody, dos users in particular, take note: if you want to play games' b'that hate/use their own upper memory manager, dos is not a problem. no' b'boot disks required. as your system starts up, hit the f5 key. this tells' b'it to bypass config and autoexec altogether. you get a plain, generic session' b"of dos, with nothing loaded. <there's another function key that actually" b"steps thru config.sys asking if you want to execute each line, but i've" b"forgotten it at the moment...you can try finding it i think it's f9..." b'' b'no, you need not bypass the config.sys, in dos there is a function' b'of multi-config, have you tried boot.sys the multi-config is the kind that' b'you can choose you config.sys at the startup. and i find that is very good.' b'it has no conflict to qemm. (i have problem when using boot.sys)' b'the key you say is f8, which is trace the config.sys step by step.' b'sorry, if any error b'' 