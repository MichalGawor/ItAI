b'' b"//i'm using bc++'s objectwindows (version and trying to get some data" b'//processed in a window object. however, when the calling program invokes' b'//the window object, it gives up the control to the window object, and keeps' b'//executing the next statement. i would like the calling program, after' b'//invoking the window object, to wait until the window object is closed.' b'//can i do that? my program may look like:' b'' b'class mywindow public twindow' b'{' b'...' b'};' b'' b'void mycallingprog(...) could the calling program be a c function?' b'{\t...' b'mywindow *mywinobj;' b'mywinobj new mywindow(...);' b'getapplication()-makewindow(mywinobj);' b'mywinobj-show(sw_shownormal);' b'' b'next statement; i want the program to wait here until mywinobj' b'... is closed so that i can get some data back from' b'... mywinobj. i specified the window style to be' b"... ws_popupwindow, didn't help. is there any other way" b'... to execute the window object so that the calling' b"... program won't give up the control? any help would" b'} be appreciated. thanks. jun' b'to talk to the lord with ps/2 through ms-windows' b'' b'' b'' 