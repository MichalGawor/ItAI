b'' b'' b"the clipper chip's user key is formed by:" b'' b'r1 e[d[e[n1;s1];s2];s1]' b'r2 e[d[e[n2;s1];s2];s1]' b'r3 e[d[e[n3;s1];s2];s1]' b'' b'why is the triple-encrytion used? is it just to gain an effective' b'increase in keyspace to defeat a potential keysearch? (if so, why use' b"80 bit keys?) not knowing anything about the skipjack algorithm, it's" b'not really possible to guess whether this makes it harder or easier to' b'guess s1,s2.' b'' b'why are n1, n2, and n3 formed as they are? it would be facinating to' b'see the skipjack algorithm, to look for ways of attacking it that require' b'three ciphertext blocks formed in that odd way.' b'' b'where do the 34-bit constant values that are concatenated with the' b'serial number to form n1,n2,n3 come from? are they changed from chip to' b"chip, or session to session? (even if they're published in the ny times," b'if skipjack is resistant to known-plaintext attacks, when using triple-' b"encryption, then there's no break in security. but why allow that kind" b'of weird format? if those three 34-bit values are truly-random bits, then' b"maybe it's used to ensure that a known-plaintext attack on skipjack, if" b"it exists, can't be easily used to derive s1 and s2 for a whole production" b'run of these chips....)' b'' b'does dorothy denning read this group? if not, is someone on the group' b'forwarding questions like these to her, or martin hellman, or anyone else' b"who's seen more details about the chip?" b'' b'--john kelsey' 