b'' b"// hope i am in the right group! i'm using bc++'s objectwindows (version b'// trying to get some date processed in a window object. however, when invoking' b'// the window object, the calling program gives up the control to the window' b'// object, and keeps executing the next statement. the source code may look' b'// like the following:' b'' b'class mywindow public twindow' b'{' b'...' b'};' b'' b'void mycallingprog(...) could the calling program be a c function?' b'{\t...' b'mywindow *mywinobj;' b'mywinobj new mywindow(...);' b'getapplication()-makewindow(mywinobj);' b'mywinobj-show(sw_shownormal);' b'' b'next statement; i want the program to wait here until mywinobj is closed' b'... so that i can get some data back from mywinobj.' b"... i specified the window style to be ws_popupwindow, didn't" b'... help. is there any other way to execute the window object' b"... so that the calling program won't give up the control?" b'} any help would be appreciated. thanks! --jun' b'to talk to the lord with ps/2 through ms-windows' b'' b'' b'' 