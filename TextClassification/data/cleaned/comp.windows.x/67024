b'' b'excerpts from netnews.comp.windows.x: 19-apr-93 monthly question about' b'' b' a button widget, when pressed, will cause a new item to be drawn in the' b' window. this action clearly should not call xcopyarea()' b' (or equiv) directly; instead, it should register the existence of the' b' new item in a memory structure and let the same expose event handler' b' that handles "regular" expose events (e.g. window manager-driven' b' exposures) take care of rendering the new image.' b'' b'hmmm.... clearly? depends on your programming model. it is not at all' b'forbidden to draw outside the context of an expose event. certainly any' b'internal data structures should be maintained such that the visual' b'appearance would be maintained properly whenever an expose event happens' b"to be generated. this doesn't preclude drawing immediately after" b'updating the datastructures though...' b'' b'-rob' b'' 