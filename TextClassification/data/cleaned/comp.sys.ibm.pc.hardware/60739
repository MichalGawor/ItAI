b'' b'(wayne smith) writes:' b'[stuff deleted]' b'so the lowly low-density original pc fdd card used dma and the pc-at' b"hdd controller doesn't!?!? that makes real sense." b'' b'actually, it does make a reasonable amount of sense. fixed disk' b'sectors are buffered by the controller, and transferring them to' b"memory with a 'rep insw' (or whatever the instruction is called) is" b'quite efficient (single instruction, goes as fast as the' b"controller/cpu know how to use the bus). since the wasn't cached," b'the bus is likely a critical resource relative to cpu performance, and' b"it's possible that dma bus interference would cause as much or more" b"loss of cpu cycles (for 'computing') as does the 'rep insw' sequence." b'' b'the floppy, on the other hand, is not buffered, so that using the cpu' b'for floppy data transfer (as was done on the pc jr, by the way) really' b'does stink.' b'' b'wally bass' 