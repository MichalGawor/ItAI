b'' b'i have a vlb machine, with a wang fax/modem.' b'when using the windows terminal application, the only way' b"i can get it to 'see' the modem is to change it to com2," b'then go back and change it to com4. then it works fine.' b"(i have it set to com4 as the port to use) i don't have" b"anything plugged into com2, and i've looked over the manual" b"for all it's worth, but what am i missing? the modem is" b"jumpered for com4/irq3, which i understand means i can't" b'use the second serial port and the modem at the same time,' b"but i'm not using the second serial port anyway." b'also, when using procomm for dos, it always sees the modem' b"and dials, but doesn't always connect me when the other end" b"answers. don't know if that helps any." b'' b'thanks,' b'' b'' 