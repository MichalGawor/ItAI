b'' b'hi,' b'' b"i'm trying to get my program to monitor resize/position events," b'so that when the program exits, i can save the last known geometry in' b"some sort of 'preferences' file." b'' b'i am adding an event handler for structurenotify to the toplevelshell:' b'' b'xtaddeventhandler(toplevel,structurenotifymask,false,configurecb,mydata);' b'' b'this works fine, and my callback is called whenever the window is resized' b'or repositioned using the window manager (mwm).' b'' b"however, when i'm just resizing the window, using any resize handle on the" b'window manager resize decoration, the x and y are set to zero in the' b"xevent passed to the event handler callback. i'm accessing the xconfigure" b'structure, like the x11 manual says. (xev-xconfigure.x, xev-xconfigure.y).' b'the width and height memebers are correct.' b'' b"when i reposition the window, using the window manager, the xevent's xconfigure" b'structure contains all the correct x, y, width, height member settings.' b'' b'q1: anybody know why, when just resizing, the x and y are being set to zero?' b'' b'q2: any other way to handle detecting and saving geometry changes?' b'' b"i've tried using xtgetvalues() on the toplevel widget when exiting my" b'application (before destroying any widgets) and all i get is garbage values' b'for the x, y, width, height resources.' b'' b'thanks' b'--' b'' b'loral instrumentation, san diego, ca again is mr. plow" homer simpson' 