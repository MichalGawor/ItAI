b'' b'' b'yes, that is a bug in the x11r5 port of xterm. you sometimes get this when' b"you start from xdm. i have reported this to casper but don't know whether" b'it made it into his set of patches yet.' b'' b'basically in mit/clients/xterm/main.c, there is a test at line which' b'checks errno after attempting to open /dev/tty. add the "errno einval"' b'case to it and you should be fine.' b'' b"it's actually a bug in the solaris kernel. we could add a workaround to" b'xterm. open should never return einval, but it does b'' b"i didn't have time to update the r5.sunos5 patch lately, but non" b'of the existing problems seem to warrant a new set of patches.' b'' b'since .xsession is a bourne shell script anyway, you might as well add a line' b'\t. /etc/timezone' b'as it is done in /etc/profile.' b'' b'xdm coulda also keep the xdm environment variable. it should have inherited' b'it from the environment.' b'' b'' b'casper' 