b'' b"| (2nd posting of the question that just doesn't seem to get answered)" b'' b'how can we resist a questions that says something like this?' b'' b'| the problem, of course, is that no expose event is generated if the window' b'| is already visible and mapped. what we need to do is somehow "tickle" the' b'| window so that the expose handler is hit with arguments that will enable' b'| it to render *just* the part of the window that contains the new item.' b'|' b'| what is the best way to tickle a window to produce this behavior?' b'' b'if your expose event handler is truly intelligent about exposed' b'rectangle information, then you can use xcleararea to generate an' b'expose event (assuming that your background pixel is not none) for the' b'enclosing rectangle of your new item.' b'' b'this is still not great, since any other items contained within that' b'rectangle will still be unnecessarily redrawn.' b'' b"if your expose event handler simply redraws everything, you'll be" b'doing much more work than just drawing the new item "on top" of the' b'existing scene.' b'' b'-----------------------------------------------------------------------' b'mike yang silicon graphics, inc.' 