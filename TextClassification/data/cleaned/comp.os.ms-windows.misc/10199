b'' b'' b"i'm writing a mail management system using word for windows as" b'a front end. the user dials up a remote system and downloads a batch' b'of mail as "foo.txt i\'ve set up an autoexec macro in winword to load' b'"foo.txt" ok, but subsequent invocations of the macro will of course' b'pose file name contention problems.' b'' b"what i'd really like to do is to create an autoexit macro in winword" b'that would move "foo.txt" to a specified directory, rename it using' b'the current date and time, and delete the original "foo.txt b'' b'in other words, "foo.txt" gets renamed as say, on may b'at the user gets a time-indexed backup of his mail files and' b'"foo.txt" is freed up for the next packet of mail, since my autoexec' b'macro points to it.' b'' b'my main question is this:' b'' b'how may one rename a file from within a wordbasic macro to include a' b'time/date variable in the new filename?' b'' b'' b'thanks-' b'' b'=======================================================================' b'"if you ever reach total enlightenment while drinking a beer,' b'i bet it makes beer shoot out of your nose." jack handey' b'-----------------------------------------------------------------------' b'=======================================================================' b'' b'' 