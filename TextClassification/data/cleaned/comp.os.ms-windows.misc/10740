b'' b'-- setup deleted...' b' finally, in order to run _your_ specific version of windows, you' b' must simply change the path to include c:\\winmastr *and* your' b' specific configuration path, e.g. c:\\winwife. you could get' b' fancy and use batch files, environment variables, or a menuing' b' system to do this.' b'--- other stuff delete...' b'if you happen to be running the new msdos you could use multi-' b'setup to provide a menu with a menu choice for each person using' b'the machine power up, select your name, the menu will use your' b'personal sections of config.sys and autoexec.bat thus setting up the' b'path, then running your windows copy!' b'ron' b'------------ temp at intel, views are my own b'' 