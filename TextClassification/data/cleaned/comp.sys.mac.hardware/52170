b'' b'' b'besides the faster processor, the has scsi and as far as i know,' b'faster i/o due to separate processors handling those functions.' b'' b"mostly. the q950 does have a scsi2 controller chip as it's scsi" b'chip, but it does not have the chips to use the wide or fast scsi-2' b"modes and therefore doesn't offer much more in the way of scsi" b'performance over the q900. it does, howrever, have a faster i/o' b'controller than the q900; this is where the real speed boost comes from.' b'' b'-hades' 