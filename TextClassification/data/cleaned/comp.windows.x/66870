b'' b'' b' i am trying to use xcopyplane to copy a single plane' b' from a depth-8 pixmap to a depth-1 pixmap. everytime i' b' try this, i see absolutely nothing displayed. i know there' b' is data in the depth-8 pixmap becuase i have checked by doing' b' an xcopyarea to the screen.' b'' b' i have been successful getting xcopyplane to work if i use' b' two pixmaps of depth-8. is there a problem with what i am' b' trying to do??' b'' b' could someone please scan my code segment and let me know' b' where i am going wrong...' b'' b' i have created a xmdrawingarea widget called canvas.' b'' b' w_id xtwindow(canvas);' b' display xtdisplay(canvas);' b' screen defaultscreen (display);' b' pixmap8 xcreatepixmap (display, rootwindow (display, screen),' b' w_width, w_height, defaultdepth (display, screen));' b' pixmap1 xcreatepixmap (display, rootwindow (display, screen),' b' w_width, w_height, b'' b' w_gc8 xcreategc (display, w_id, null);' b' w_gc1 xcreategc (display, pixmap1, null);' b'' b' code to read xwd image into pixmap8 is omitted b'' b'' b'have you set the foreground and background colors in w_gc1 to' b'something other than and the whitepixel and blackpixel macros' b'on your server may not return values suitable for depth drawables.' b'' b'' b' copy one plane into the depth-1 pixmap b' xcopyplane (display, pixmap8, pixmap1,' b' w_gc1, w_width, w_height, b'' b'' b"are you sure that the fifth plane of the data isn't all the same? you could" b'have different pixel values in the image, but the fifth plane (0x10 b'might all be the same value?' b'' b'' b' using the depth-1 pixmap as the clipmask, copy it to the screen b' values.foreground fg_color;' b' values.clip_x_origin b' values.clip_y_origin b' values.clip_mask pixmap1;' b' mask gcforeground gcclipmask;' b' xchangegc (display, w_gc8, mask, &values);' b'' b' xfillrectangle (display, w_id, w_gc8, w_width, w_height);' b'' b'' b'' b'if you just want to see the plane of data and want better performance,' b"don't use clipmasks, just use pixmap1 as the stipple in a stippled" b'rectangle fill. many servers are very stupid about handling complex' b'clip lists, and turn a bitmap clip region into hundreds and hundreds of' b'little clipping rectangles, and clips every drawing primitive against every' b'one of these little triangles.' b'' b'' b"actually, i must also ask the faq's most popular reason why graphics" b"don't show up: do you wait for an expose event before drawing your" b'rectangle?' b'' b'' b'' b' other info: x11r5 patchlevel b' motif b' sun sparc2 running sunos b'' b'' b' thanks in advance!' b'' b' b' ensco inc. mail: pineda ct. melbourne, fl. b' fax: b' b'--' b'kenneth whaley\t\t\t b'2630 walsh avenue' b'santa clara, ca. 