b'' b'' b'[hint for sun os users: use /usr/5bin/echo instead of' b"/bin/echo or csh's built-in echo. otherwise you'll have" b'to embed literal esc and bel characters in the string' b'instead of using convenient octal sequences.]' b'' b'bad idea. using /usr/5bin/echo is slower than the built-in echo.' b'this is what i do. it does execute `hostname` once per shell window,' b'and does read in one extra file. but manipluating the titles does not' b'require executing extra programs.' b'' b'oh yes, it does execute some programs once per each system.' b'but it stores them in a file for the next time...' b'' b'' b'if $?hostname then' b'setenv hostname `hostname`' b'endif' b'' b'# figure how how to generate escape, bell,' b'# and echo commands without a a line terminator' b'# i may have done this before. if so, the variable e is set' b'' b'# have i executed this script before on this system?' b'if $?e then' b'#\techo "already set the echo variables"/dev/tty' b'else if -f ~/.echo.${hostname} then' b'source ~/.echo.${hostname}' b'else if `echo -n |wc -l` then' b'#\techo "built in echo is bsd" /dev/tty' b'# then berkeley style echo' b'echo \'set ech "echo -n"\' ~/.echo.${hostname}' b'echo "set e `echo a tr a ~/.echo.${hostname}' b'echo "set b `echo a tr a ~/.echo.${hostname}' b'echo \'set n ~/.echo.${hostname}' b'source ~/.echo.${hostname}' b'else' b'#\techo "built in echo is sysv" /dev/tty' b'echo \'set ech "echo"\' ~/.echo.${hostname}' b'echo \'set e ~/.echo.${hostname}' b'echo \'set b ~/.echo.${hostname}' b'echo \'set n "\\c"\' ~/.echo.${hostname}' b'source ~/.echo.${hostname}' b'endif' b'' b'' b'# are we using shelltool, cmdtool or xterm?' b'# duplicate these aliases here to avoid problems' b'if $term sun* then' b'# sun aliases' b'alias header \'${ech} "${e}]l\\!:1${e}\\${n}"\'' b'alias iheader \'${ech} "${e}]l\\!:1${e}\\${n}"\'' b'else if $term xterm then' b'alias header \'${ech} "${e}]2;\\!:1${b}${n}"\'' b'alias iheader \'${ech} "${e}]1;\\!:1${b}${n}"\'' b'endif' b'' b'' b'' b'--' 