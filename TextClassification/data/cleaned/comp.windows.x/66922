b'' b' to get the number back, the other client does:' b'' b' appatom xinternatom(display,"applicationwindow",true);' b' xgetwindowproperty(display,defaultrootwindow(display),appatom,0,8192,' b' false,xa_window,&return_type,&return_format,' b' &nitems_return,&bar,&return_place);' b'' b' and appears to get back something valid (just not the right number).' b' it always seems to return the same number, regardless of the window' b' number stored in the property.' b'' b'"return_place" is probably incorrect. it should be a pointer, not an' b'integer. xgetwindowproperty() allocates memory, copies the data there,' b'and returns a pointer to the memory. you should free the memory when' b"you're done." b'' b'---' 