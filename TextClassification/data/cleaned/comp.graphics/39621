b'' b')many high-end graphics cards come with c source code for doing basic graphics' b')sorts of things (change colors, draw points/lines/polygons/fills, etc.). does' b')such a library exist for generic vga graphics cards/chips, hopefully in the' b')public domain? this would be for the purpose of compiling under a non-dos' b')operating system running on a standard pc.' b'' b'check the server code for x11r5. (or "xfree86")' b'--' b'daniel "un?x" ortmann (talmidim) ndsu electrical engineering' 